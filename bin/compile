#!/usr/bin/env bash
# bin/compile <build-dir> <cache-dir> <env-dir>

set -e

build_dir=$1
cache_dir=$2
env_dir=$3
bp_dir=$(dirname $(dirname $0))

mkdir $build/ngx_mruby
cd ngx_mruby
git clone https://github.com/matsumotory/ngx_mruby.git
cd ngx_mruby
sh ./build.sh
make install

exit 0
